# AchievementsPage

A server component that fetches all achievements from the database and renders them in a 3-column responsive grid. Between the title and the grid sits a controls row (rendered via the shared Controls component) with category/sort filters and admin actions.
