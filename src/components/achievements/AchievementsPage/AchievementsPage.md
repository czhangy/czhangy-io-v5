# AchievementsPage

A server component that fetches all achievements from the database and renders them in a 3-column responsive grid. Between the title and the grid sits a controls row (AchievementsControls) for admin actions and future filters.
