# LogsContent

Client component rendered by LogsPage. Currently renders only the controls bar, with an admin-only "Add Log" button that navigates to the new-log route. Will grow to display the list of log entries once that server-side data is wired up.
