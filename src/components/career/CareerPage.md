# CareerPage

A full-page vertical timeline of job history. Reads jobs from the database and renders each entry as a JobCard connected to a shared timeline line, with admin controls for adding, editing, and deleting entries.
