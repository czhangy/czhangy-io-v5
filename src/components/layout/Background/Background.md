# Background

A fixed full-viewport overlay that renders the site-wide background effects: a purple radial glow, scanlines, and a sweep line. Mounted once in the root layout so every page inherits it automatically. Uses `pointer-events: none` so it never blocks interaction.
